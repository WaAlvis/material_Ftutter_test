part of 'splash_view.dart';

class _SplashWeb extends StatelessWidget {
  const _SplashWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
