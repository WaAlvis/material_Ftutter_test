part of 'splash_view.dart';

class _SplashMobile extends StatelessWidget {
  const _SplashMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
